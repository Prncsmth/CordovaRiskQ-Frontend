// Responder-only additions to the shared theme (see theme/colors.ts). Used
// exclusively by app/responder/* and components/responder/* so the citizen
// app's light red/teal look is untouched.
export const RESPONDER_COLORS = {
  surfaceDark: "#17181A",
  textOnDark: "#F2F4F5",
  textOnDarkMuted: "#9CA3AF",
};
